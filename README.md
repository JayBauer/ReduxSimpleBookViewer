# ReduxSimpleBookViewer

Covered the basics of Redux using React. Simple book viewer with static data that displays a list of books and displays details when one is selected. Defaults to no selection.
